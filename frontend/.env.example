# ローカル開発ではフロント(5173)とバックエンド(8080)が別オリジンになるため、絶対URLを指定する。
VITE_API_BASE_URL=http://localhost:8080/api

# デプロイ時は相対パス(/api)にする。
# CloudFrontが静的ファイルとAPIを同じドメインから配信するため同一オリジンになり、
# 絶対URLを埋め込む必要がない(docs/aws-architecture.md 参照)。
#
#   VITE_API_BASE_URL=/api npm run build
